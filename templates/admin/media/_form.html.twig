<div class="form">
	{{ form_start(form) }}
	    <div class="errors">
	        {{ form_errors(form) }}
	    </div>

	    {% include 'admin/forms/input.html.twig' with {field: form.code, width: 'short'} %}
	    {% include 'admin/forms/input.html.twig' with {field: form.imageFile} %}

	    <button type="submit" class="save">Save</button>

	{{ form_end(form) }}
</div>